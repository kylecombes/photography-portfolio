services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      THUMB_WIDTH: ${THUMB_WIDTH:-600}
      FULL_QUALITY: ${FULL_QUALITY:-70}
      # Path to a GeoLite2-City.mmdb inside the container (optional). When unset,
      # analytics geolocation is disabled and city/region/country stay null.
      GEOLITE_DB: ${GEOLITE_DB:-}
    volumes:
      # Read-only bind mount of the host ingest folder; originals are never modified.
      - ${INGEST_DIR}:/ingest:ro
      # Generated derivatives (full + thumb) live on a named volume.
      - processed:/processed
      # Optional: mount a GeoLite2-City database and set GEOLITE_DB to its path.
      # - ./GeoLite2-City.mmdb:/data/GeoLite2-City.mmdb:ro
    expose:
      - '3000'

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - web
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN:-:80}
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  pgdata:
  processed:
  caddy_data:
  caddy_config:
